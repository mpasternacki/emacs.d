;; Packages that need only loading, no customisation.

;; native, needed? (require 'cc-vars)
(require 'sierotki)                     ; not in packages
(require 'filladapt)                    ; not in packages

;;; native
(require 'ido)
(require 'uniquify)

;;; TODO: AucTeX (from ports? from package??)
(load "auctex")
(load "preview-latex")
